<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex flex-col items-center min-h-screen">

    <!-- Sticky Navbar -->
    <nav class="bg-blue-600 text-white w-full py-4 px-6 shadow-md fixed top-0 left-0 z-50">
        <h1 class="text-xl font-bold text-center">QRC for the Contact</h1>
    </nav>

    <!-- Main Content -->
    <div class="mt-16 p-6 w-full max-w-3xl bg-white shadow-lg rounded-lg">
        <h2 class="text-2xl font-bold mb-6 text-center">Salesforce Contact</h2>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <!-- Left Column: Contact Details -->
            <div>
                <table class="w-full border-collapse border border-gray-300">
                    <tr class="bg-gray-200">
                        <td class="border border-gray-300 p-2 font-semibold">User:</td>
                        <td class="border border-gray-300 p-2"><%= context.user.fullName %></td>
                    </tr>
                    <tr>
                        <td class="border border-gray-300 p-2 font-semibold">Object:</td>
                        <td class="border border-gray-300 p-2"><%= context.environment.record.attributes.type %></td>
                    </tr>
                    <tr class="bg-gray-200">
                        <td class="border border-gray-300 p-2 font-semibold">Id:</td>
                        <td class="border border-gray-300 p-2"><%= context.environment.record.Id %></td>
                    </tr>
                </table>
            </div>

            <!-- Right Column: QR Code -->
            <div class="flex flex-col items-center justify-center">
                <h3 class="text-lg font-semibold mb-4">QR Code</h3>
                <div class="border border-gray-300 p-2 shadow-lg rounded-lg">
                    <%- imgTag %>
                </div>
            </div>
        </div>
    </div>

</body>
</html>
